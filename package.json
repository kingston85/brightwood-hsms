{
  "name": "brightwood-hsms-functions",
  "description": "Stripe card/Google Pay checkout backend for Brightwood HSMS — see README.md \"Card & Google Pay Setup (Stripe)\".",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.0.0",
    "firebase-functions": "^7.0.0",
    "stripe": "^22.0.0"
  }
}
